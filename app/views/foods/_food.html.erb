<tr>
  <td><%= food.name %></td>
  <td><%= food.measurement_unit %></td>
  <td><%= "$ #{food.price}" %></td>
  <td><%= " #{food.quantity} units" %></td>
  <td>
    <%= button_to "Delete", user_food_url(user_id: @users.id, id: food.id), method: :delete, class: 'destroy_food' %>
  </td>
</tr>
